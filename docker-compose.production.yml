services:
  # FastAPI as the backend/middleware
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
      target: production
    ports:
      - 7100:7100

  # SvelteKit as the frontend
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      target: production
    ports:
      - 80:80
    depends_on:
      - backend
